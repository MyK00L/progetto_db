{% extends "base" %}
{% block title %}{{name}}{% endblock title %}
{% block content %}

<h2>Inserisci {{name}}</h2>
<form method="post" action="/api/insert">
	<input required type="hidden" name="table" value="{{name}}" />
	{% for col in cols %}
	<div class="form-group">
		<label for="columns.{{col.name}}">{{col.name}}</label>
		<input {% if col.is_required %} required {% endif %} type="{{col.type}}" class="form-control" id="columns.{{col.name}}" name="columns.{{col.name}}"/>
  </div>
	{% endfor %}
	<button type="submit" class="btn btn-primary">Inserisci</button>
</form>

{% endblock content %}
